<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="AniMeido.Plugin.Base.Views.Controls.AnimeCard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AniMeido.Plugin.Base.Views.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Width="150"
            Margin="4">
        <Border Background="{ThemeResource CardBackgroundFillColorSecondary}"
                CornerRadius="8"
                Translation="0,0,32">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- 封面图片 -->
                <Border Grid.Row="0"
                        CornerRadius="8,8,0,0"
                        Height="200">
                    <Border.Clip>
                        <RectangleGeometry Rect="0,0,150,200"/>
                    </Border.Clip>
                    <Image Source="{Binding CoverURL}"
                            Height="200"
                            Stretch="UniformToFill"/>
                </Border>

                <!-- 文字信息 -->
                <StackPanel Grid.Row="1"
                        Padding="8,4">
                    <TextBlock Text="{Binding Title}"
                            FontWeight="SemiBold"
                               TextTrimming="CharacterEllipsis"
                            MaxLines="2"
                               Margin="0,0,0,2"/>
                    <TextBlock Text="{Binding AirDate}"
                            FontSize="12"
                            Foreground="Gray"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
