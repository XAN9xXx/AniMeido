<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="AniMeido.Plugin.Base.Views.Controls.AnimeCard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AniMeido.Plugin.Base.Views.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Width="150" Margin="4">
        <StackPanel>
            <Image Source="{Binding CoverURL}" Height="200" Stretch="UniformToFill" />
            <TextBlock Text="{Binding Title}" FontWeight="SemiBold" Margin="0,4,0,0" />
            <TextBlock Text="{Binding AirDate}" FontSize="12" Foreground="Gray" />
        </StackPanel>
    </Grid>
</UserControl>
