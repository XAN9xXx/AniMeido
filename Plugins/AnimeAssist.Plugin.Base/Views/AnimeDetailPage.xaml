<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="AniMeido.Plugin.Base.Views.AnimeDetailPage"
    x:Name="RootPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:contracts="using:AniMeido.Contracts.Models"
    xmlns:local="using:AniMeido.Plugin.Base.Views"
    xmlns:vm="using:AniMeido.Plugin.Base.ViewModels"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 错误提示 -->
        <InfoBar x:Name="ErrorInfoBar"
                 Grid.Row="0"
                 Margin="24,16,24,0"
                 Severity="Error"
                 IsClosable="True"
                 IsOpen="False"/>

        <!-- 内容区域 -->
        <ScrollViewer Grid.Row="1"
                      x:Name="ContentScrollViewer">
            <Grid Padding="24,20,24,24">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="300"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- 左列：封面图 -->
                <Border Grid.Column="0"
                        CornerRadius="12"
                        Margin="0,0,24,0"
                        Width="300"
                        MaxHeight="440"
                        VerticalAlignment="Top"
                        Style="{StaticResource AniMeidoCardStyle}"
                        Padding="0">
                    <Image x:Name="DetailCoverImage"
                           Source="{Binding AnimeDetail.CoverURL}"
                           Stretch="Uniform"
                           ImageFailed="OnDetailCoverImageFailed"/>
                </Border>

                <!-- 右列：信息 -->
                <StackPanel Grid.Column="1"
                            Spacing="16">

                    <!-- 标题 -->
                    <TextBlock Text="{Binding AnimeDetail.Title}"
                               Style="{StaticResource AniMeidoPageTitleStyle}"
                               TextWrapping="Wrap"/>

                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <Button x:Name="OnlinePlayButton"
                                Click="OnOnlinePlayClick"
                                IsEnabled="{Binding HasData}"
                                Visibility="Collapsed"
                                Style="{StaticResource AccentButtonStyle}"
                                Padding="16,8">
                            <StackPanel Orientation="Horizontal" Spacing="6">
                                <FontIcon Glyph="&#xE768;"/>
                                <TextBlock Text="在线播放"/>
                            </StackPanel>
                        </Button>

                        <Button Click="OnPersonalArchiveClick"
                                IsEnabled="{Binding HasData}"
                                Padding="16,8">
                            <StackPanel Orientation="Horizontal" Spacing="6">
                                <FontIcon Glyph="&#xE8F1;"/>
                                <TextBlock Text="个人档案"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>

                    <!-- 制作/原作 -->
                    <TextBlock x:Name="DetailStudio"
                               FontSize="14"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                               Visibility="Collapsed"/>

                    <!-- 分割线 -->
                    <Border Height="1"
                            Background="{ThemeResource DividerStrokeColorDefault}"/>

                    <!-- 放送日期 -->
                    <TextBlock Text="{Binding AnimeDetail.AirDate}"
                               FontSize="14"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>

                    <!-- 评分 -->
                    <TextBlock x:Name="DetailScore"
                               FontSize="14"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                               Visibility="Collapsed"/>

                    <!-- 简介 -->
                    <Border Style="{StaticResource AniMeidoContentCardStyle}">
                        <StackPanel Spacing="10">
                            <TextBlock Text="简介"
                                       Style="{StaticResource AniMeidoSectionTitleStyle}"/>
                            <TextBlock Text="{Binding AnimeDetail.Description}"
                                       FontSize="15"
                                       TextWrapping="Wrap"
                                       LineHeight="24"/>
                        </StackPanel>
                    </Border>

                    <!-- 角色区 -->
                    <StackPanel x:Name="CharacterSection"
                                Visibility="Collapsed"
                                Spacing="8">
                        <TextBlock Text="角色"
                                   Style="{StaticResource AniMeidoSectionTitleStyle}"/>
                        <ScrollViewer HorizontalScrollMode="Auto"
                                      VerticalScrollMode="Disabled"
                                      HorizontalScrollBarVisibility="Hidden">
                            <StackPanel x:Name="CharacterPanel"
                                        Orientation="Horizontal"
                                        Spacing="8"/>
                        </ScrollViewer>
                    </StackPanel>

                    <!-- 分割线 -->
                    <Border Height="1"
                            Background="{ThemeResource DividerStrokeColorDefault}"/>

                    <!-- 关注状态按钮 -->
                    <ScrollViewer
                        HorizontalScrollBarVisibility="Hidden"
                        HorizontalScrollMode="Auto"
                        VerticalScrollMode="Disabled">
                        <ItemsControl ItemsSource="{x:Bind ViewModel.VisibleTrackingActions, Mode=OneWay}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <ItemsStackPanel Orientation="Horizontal"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate x:DataType="vm:TrackingActionDescriptor">
                                    <ToggleButton
                                        Margin="0,0,8,8"
                                        Padding="14,8"
                                        BorderBrush="{ThemeResource ControlStrokeColorDefault}"
                                        BorderThickness="1"
                                        Command="{Binding ElementName=RootPage, Path=ViewModel.SetTrackingStatusCommand}"
                                        CommandParameter="{x:Bind Status}"
                                        CornerRadius="8"
                                        IsChecked="{x:Bind IsSelected, Mode=OneWay}"
                                        IsEnabled="{Binding ElementName=RootPage, Path=ViewModel.HasData}">
                                        <StackPanel Orientation="Horizontal" Spacing="6">
                                            <FontIcon
                                                FontSize="14"
                                                Glyph="{x:Bind Glyph}"/>
                                            <TextBlock
                                                FontSize="14"
                                                FontWeight="SemiBold"
                                                Text="{x:Bind Label}"/>
                                        </StackPanel>
                                    </ToggleButton>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>

                    <!-- 当前状态提示 -->
                    <TextBlock
                        FontSize="13"
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                        Text="{x:Bind ViewModel.CurrentStatusHint, Mode=OneWay}"
                        Visibility="{x:Bind local:AnimeDetailPage.BooleanToVisibility(ViewModel.HasCurrentStatus), Mode=OneWay}"/>

                    <!-- 分割线 -->
                    <Border Height="1"
                            Background="{ThemeResource DividerStrokeColorDefault}"/>

                    <!-- 标签区域 -->
                    <TextBlock Text="Bangumi 标签"
                               Style="{StaticResource AniMeidoSectionTitleStyle}"/>
                    <StackPanel x:Name="TagContainer"
                                Orientation="Horizontal"
                                Visibility="Collapsed">
                    </StackPanel>

                    <!-- 分割线 -->
                    <Border Height="1"
                            Background="{ThemeResource DividerStrokeColorDefault}"/>

                    <!-- Bangumi 百科入口 -->
                    <Border x:Name="BangumiCard"
                            Style="{StaticResource AniMeidoContentCardStyle}"
                            Padding="14,10"
                            IsTapEnabled="True"
                            Tapped="OnBangumiCardTapped"
                            PointerEntered="OnBangumiPointerEntered"
                            PointerExited="OnBangumiPointerExited"
                            PointerPressed="OnBangumiPointerPressed"
                            PointerReleased="OnBangumiPointerReleased"
                            HorizontalAlignment="Left">
                        <StackPanel Orientation="Horizontal"
                                    Spacing="8">
                            <FontIcon Glyph="&#xE774;"
                                      FontSize="14"
                                      Foreground="{ThemeResource SystemAccentColor}"
                                      VerticalAlignment="Center"/>
                            <TextBlock Text="查看 Bangumi 百科条目"
                                       VerticalAlignment="Center"
                                       FontSize="14"
                                       FontWeight="SemiBold"/>
                            <FontIcon Glyph="&#xE2B1;"
                                      FontSize="12"
                                      Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                      VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>

                </StackPanel>
            </Grid>
        </ScrollViewer>

        <!-- 加载/重试覆盖层 -->
        <Grid Grid.Row="1"
              x:Name="LoadingOverlay"
              Visibility="Collapsed"
              Background="Transparent"
              Tapped="OnLoadingOverlayTapped">
            <StackPanel HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Spacing="12">
                <ProgressRing x:Name="LoadingRing"
                              Height="40"
                              Width="40"
                              HorizontalAlignment="Center"/>
                <Image x:Name="LoadingFailedImage"
                       Source="ms-appx:///Assets/Loading_failed.png"
                       Width="80"
                       Height="80"
                       Stretch="Uniform"
                       HorizontalAlignment="Center"
                       Visibility="Collapsed"/>
                <TextBlock x:Name="LoadingHint"
                           FontSize="14"
                           Foreground="{ThemeResource AniMeidoMutedTextBrush}"
                           HorizontalAlignment="Center"
                           TextWrapping="Wrap"
                           MaxWidth="300"
                           TextAlignment="Center"/>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
